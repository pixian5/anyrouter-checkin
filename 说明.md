# 本项目部署与签到说明

上游自带 `README.md`，本仓库的服务器定制统一记录在本文件，避免修改上游说明造成合并冲突。

## 运行边界

- 仅 clone 仓库不会自动运行。
- 在某台服务器手动执行 `python -m pixian_overlay.runner`，只会在该服务器签到。
- 在某台服务器执行 `deploy/install-local-systemd.sh` 并启用服务，只有该服务器的 timer 会定时签到。
- 上游 workflow 文件保持原文；本仓库的 GitHub Actions 权限已关闭，因此不会从 GitHub 运行签到或远程触发服务器。
- 安装器按当前项目目录生成 systemd unit，不绑定服务器域名或固定 `/opt` 路径。

## 服务器安装

在目标服务器的项目目录准备 `.env` 与项目 `.venv` 后执行：

```bash
sudo ./deploy/install-local-systemd.sh
```

`.venv` 按上游 `pyproject.toml`/`uv.lock` 创建；这两个文件始终保持上游原文。根目录 `VERSION` 仅表示 pixian 外挂版本，不会回写上游项目版本。

查看实际安装路径和运行状态：

```bash
systemctl show anyrouter-checkin.service -p WorkingDirectory -p ExecStart
systemctl status anyrouter-checkin.timer --no-pager
journalctl -u anyrouter-checkin.service -n 200 --no-pager
```

默认在 Asia/Singapore 时区的 11:00、17:00、23:00 尝试。首次失败不会写入“今日已签到”，后续时间点会继续重试。

## 成功判定

AnyRouter 主动签到不能只相信 HTTP 200 或“签到成功”文本，必须满足以下之一：

1. 接口明确返回“今日已签到”；或
2. 余额和累计消耗证明奖励实际到账。

奖励计算公式：

```text
实际奖励 = 签到后余额 - 签到前余额 + 签到后累计消耗 - 签到前累计消耗
```

如果同时消费 $25、签到增加 $25，余额前后不变，但累计消耗增加 $25，公式仍得到奖励 $25。若只有消费没有签到，余额减少 $25、累计消耗增加 $25，结果为 $0，不会误判成功。

接口返回 success 但第一次余额没有变化时，会间隔两秒只读复查三次。仍没有正奖励就返回失败、不保存当日成功状态，并发送失败通知。

## 路由与通知

- 签到程序不选择代理节点；AnyRouter 域名路由由每台服务器自己的 sing-box 配置负责。
- 当天已成功的邮箱账号不会重复启动浏览器。
- Cookie 账号当天已成功后只读刷新余额，用于发现外部签到带来的余额变化。
- 仅余额发生变化或存在签到失败时发送通知；所有账号已签到且余额不变时不通知。

## 上游文件保护

所有在 `upstream/main` 已存在的文件都必须保持原文，定制只能新增外挂文件。提交和部署前执行：

```bash
./scripts/verify_upstream_unchanged.sh
```

脚本会使用临时 Git index 对比工作树中的每个上游文件；任何内容、权限或删除差异都会失败。仓库设置中关闭 Actions 是仓库级配置，不需要删除或修改上游 workflow。
