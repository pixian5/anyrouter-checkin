# 本项目部署与签到说明

上游自带 `README.md`，本仓库的服务器定制统一记录在本文件，避免修改上游说明造成合并冲突。

当前 pixian 外挂版本为 `0.3.9`。本文覆盖运行边界、账号登录方式、定时计划、成功判定、重试、通知、sing-box 路由、服务器部署、测试和上游保护。`.env`、账号密码、session、订阅地址和节点凭据属于敏感信息，只保存在服务器，不写入本文或 Git。

## 运行边界

- 仅 clone 仓库不会自动运行。
- 在某台服务器手动执行 `python -m pixian_overlay.runner`，只会在该服务器签到。
- 在某台服务器执行 `deploy/install-local-systemd.sh` 并启用服务，只有该服务器的 timer 会定时签到。
- 上游 workflow 文件保持原文；本仓库的 GitHub Actions 权限已关闭，因此不会从 GitHub 运行签到或远程触发服务器。
- 安装器按当前项目目录生成 systemd unit，不绑定服务器域名或固定 `/opt` 路径。

## 服务器安装

在目标服务器的项目目录准备 `.env` 与项目 `.venv` 后执行：

```bash
sudo ./deploy/install-local-systemd.sh
```

`.venv` 按上游 `pyproject.toml`/`uv.lock` 创建；这两个文件始终保持上游原文。根目录 `VERSION` 仅表示 pixian 外挂版本，不会回写上游项目版本。

查看实际安装路径和运行状态：

```bash
systemctl show anyrouter-checkin.service -p WorkingDirectory -p ExecStart
systemctl status anyrouter-checkin.timer --no-pager
journalctl -u anyrouter-checkin.service -n 200 --no-pager
```

默认在 Asia/Singapore 时区每天 08:00、14:00、20:00 尝试。AnyRouter 的签到日界线经真实运行确认是 08:00：07:55 接口虽然返回 success，但余额没有增加；08:00 后重试余额从 $6976.30 增至 $7001.30，实际到账 $25。因此第一轮定在日界线时刻，14:00、20:00 用于失败兜底。首次失败不会写入“今日已签到”，后续时间点会继续重试。

timer 模板：

```ini
OnCalendar=*-*-* 08,14,20:00:00 Asia/Singapore
Persistent=true
```

安装或更新 timer 后验证：

```bash
systemctl list-timers anyrouter-checkin.timer --no-pager
```

`deploy/install-local-systemd.sh` 已负责 `daemon-reload` 与 `enable --now`。日常更新不要额外重启已激活的 timer，避免 `Persistent=true` 把错过的时间点立即补跑；只有明确希望立即补跑时才手动启动 `anyrouter-checkin.service`。

## 成功判定

AnyRouter 主动签到不能只相信 HTTP 200 或“签到成功”文本，必须满足以下之一：

1. 接口明确返回“今日已签到”；或
2. 余额和累计消耗证明奖励实际到账。

奖励计算公式：

```text
实际奖励 = 签到后余额 - 签到前余额 + 签到后累计消耗 - 签到前累计消耗
```

如果同时消费 $25、签到增加 $25，余额前后不变，但累计消耗增加 $25，公式仍得到奖励 $25。若只有消费没有签到，余额减少 $25、累计消耗增加 $25，结果为 $0，不会误判成功。

累计消耗计数器若被月度或后台策略重置，负向 `used` 差值不会抵消已观察到的正向余额增加：此时只以正向余额增量作为最低奖励证据；余额不增加而只有计数器归零时仍判定为无奖励。

AgentRouter 没有独立签到接口，首次读取用户资料就可能自动触发签到，因此同一次运行内的“签到前”已经可能是到账后的值。外挂会把上一轮的 `quota + used` 作为独立奖励证据基线：只有当前资料相对该基线得到正奖励才写成功状态。未确认运行只更新通知用的最新余额，不覆盖证据基线；若奖励延迟到账，14:00 或 20:00 仍可继续确认。全新部署没有历史基线时，第一轮只建立基线、不冒充成功，下一签到日开始即可严格比较。

接口返回 success 但第一次余额没有变化时，会间隔两秒只读复查三次。仍没有正奖励就返回失败、不保存当日成功状态，并发送失败通知。

实际验证记录：

- 2026-08-17 07:55：AnyRouter 返回 HTTP 200/success，余额仍为 $6976.30、累计消耗 $48.70；程序判定失败，没有写成功状态。
- 2026-08-17 08:00：同一正式 systemd 服务重试，余额从 $6976.30 增至 $7001.30、累计消耗保持 $48.70；确认奖励 $25，服务退出码为 0。
- `hqlak47` 与 `g_sbbz` 当日余额均为 $500；成功后后续执行直接跳过邮箱浏览器登录。
- 2026-08-17 14:00、20:00：三账号状态均保持成功；邮箱账号未再次启动浏览器，AnyRouter 只读余额仍为 $7001.30、累计消耗 $48.70；余额无变化、没有失败，因此没有发送通知，20:00 服务退出码为 0。

## 账号登录方式

- `hqlak47`、`g_sbbz`：由项目程序使用邮箱和密码启动无头浏览器登录 AgentRouter；进入控制台后通过 `/api/user/self` 确认登录身份。资料接口瞬时空响应会在原超时窗口内每两秒重试。
- `85976`：使用服务器 `.env` 中的 AnyRouter session Cookie；每次运行由浏览器获取新的 WAF Cookies，再由项目程序调用用户信息和签到接口。
- 所有签到均由 `pixian_overlay.runner` 执行，没有人工点击签到，也没有额外手写的独立签到程序。

## 路由与通知

- 签到程序不选择代理节点；AnyRouter 域名路由由每台服务器自己的 sing-box 配置负责。
- sf 的 sing-box 在签到服务启动前按 `anyrouter.top` 对订阅节点进行 URLTest 预选；路由和节点选择属于服务器 sing-box，不属于签到程序。
- 当天已成功的邮箱账号不会重复启动浏览器。
- Cookie 账号当天已成功后只读刷新余额，用于发现外部签到带来的余额变化。
- 仅余额发生变化或存在签到失败时发送通知；所有账号已签到且余额不变时不通知。
- 通知会尝试所有渠道，并明确记录是否至少有一个渠道交付成功；某个未配置渠道不影响 Bark 等其他渠道。

## 状态、重试与通知

- 当日成功状态保存在项目目录的 `daily_checkin_state.json`，余额通知基线保存在 `balance_hash.txt`。
- 只有明确“今日已签到”或奖励公式得到正奖励，才会写成功状态。
- 失败账号不会写成功状态；14:00、20:00 会继续重试。已成功账号会按账号级状态跳过，不影响失败账号重试。
- 两个状态文件都使用同目录临时文件、`flush + fsync + os.replace` 原子写入；写入失败会使服务返回非零，旧文件不会被截断。成功账号状态先于余额基线保存，防止已到账账号因基线写失败而重复签到。
- 状态 JSON 损坏、出现 `NaN/Infinity` 或结构错误时会在任何账号网络请求前终止并报警，不会把损坏状态当成“今天未签到”。
- 账号稳定键重复、provider 未知、URL/路径/账号字段无效时在网络请求前终止，避免两个账号覆盖同一状态或部分账号先行签到。
- `pixian_overlay.runner` 使用按项目路径区分的进程锁；systemd、手动命令或其他入口重叠时，仅保留一个实际签到进程。
- 任一账号失败时进程返回非零并发送失败通知；余额有变化时发送通知；所有账号已成功且余额不变时不发送通知。
- GitHub Actions 已在仓库设置中关闭，签到只由明确安装并启用的服务器本机 systemd timer 执行。
- sf 当前以 Bark 为有效通知渠道；其他未配置渠道会记录跳过或配置缺失信息，不影响 Bark 和签到结果。

## sf 当前部署与验证

- 项目目录：`/opt/anyrouter-checkin`
- 服务入口：`.venv/bin/python -m pixian_overlay.runner`
- timer：`anyrouter-checkin.timer`
- sing-box 预选 drop-in：`anyrouter-checkin.service.d/proxy-preselect.conf`
- 时区：`Asia/Singapore`
- 服务器运行环境：Python 3.14；已安装 pytest、pytest-asyncio、pytest-mock、pytest-cov、Ruff、MyPy、Bandit。
- 本轮本地与 sf 全量测试均为：112 passed、2 skipped；两个跳过项是未启用的真实通知接口测试。两端 Ruff、MyPy、Bandit、compileall、shell 语法、`pip check` 与上游逐文件保护检查均通过。
- 0.3.9 部署后 21:22 正式 systemd 运行中，两个邮箱账号没有启动浏览器，AnyRouter 没有发送签到请求；首次只读查询遇到瞬时 TLS EOF，程序保留已确认状态与旧余额、未误报失败、未通知并退出 0。21:24 再次用同一 unit 复核，只读查询成功，余额仍为 $7001.30、累计消耗 $48.70，仍未通知且退出 0。
- `pip check` 无依赖冲突；上游逐文件保护检查通过。

常用命令：

```bash
systemctl start anyrouter-checkin.service
systemctl status anyrouter-checkin.service --no-pager
journalctl -u anyrouter-checkin.service -n 200 --no-pager
systemctl list-timers anyrouter-checkin.timer --no-pager
.venv/bin/python -m pytest tests/ -q
./scripts/verify_upstream_unchanged.sh
```

若服务器虚拟环境缺少测试依赖，安装命令为：

```bash
.venv/bin/python -m pip install --upgrade \
  "pytest>=7.4.0" \
  "pytest-asyncio>=0.21.0" \
  "pytest-mock>=3.11.1" \
  "pytest-cov>=4.1.0"
```

## 上游文件保护

所有在 `upstream/main` 已存在的文件都必须保持原文，定制只能新增外挂文件。提交和部署前执行：

```bash
./scripts/verify_upstream_unchanged.sh
```

脚本会使用临时 Git index 对比工作树中的每个上游文件；任何内容、权限或删除差异都会失败。仓库设置中关闭 Actions 是仓库级配置，不需要删除或修改上游 workflow。
